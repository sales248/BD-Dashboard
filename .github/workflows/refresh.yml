name: Refresh BD Dashboard

on:
  # Manual trigger from the GitHub Actions tab — no token required
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Dashboard refresh
        run: |
          echo "================================================"
          echo "  OA BD Dashboard — Manual Refresh Triggered"
          echo "================================================"
          echo ""
          echo "  To refresh live data, ask Claude:"
          echo "  'Refresh my BD dashboard'"
          echo ""
          echo "  Claude will fetch fresh HubSpot data and"
          echo "  push updated data.json to this repo."
          echo "================================================"
